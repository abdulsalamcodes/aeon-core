# schooler-core environment
NODE_ENV=development
PORT=8080

# Postgres (system of record). In prod, point at PgBouncer (transaction pooling).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/schooler_core

# Redis (cache, queue, event streams)
REDIS_URL=redis://localhost:6379

LOG_LEVEL=info
